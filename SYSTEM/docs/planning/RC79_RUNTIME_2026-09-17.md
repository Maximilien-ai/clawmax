# RC79 runtime follow-up — 2026-09-17

Status: candidate CI, full local validation, and both image pipelines passed.
The pinned combined RC79 image is installed on MBP14 and test10; gateway-aware
health and all-workspace agent/workflow ID preservation passed. Fresh MBP14 chat,
reply persistence, and Markdown download passed. Cloud fresh-chat acceptance
is blocked on BYOK configuration in the isolated browser; broader rollout is held.
Operations deployment remains excluded and its four
recurring workflows were verified disabled during RC78 acceptance.

## MBP14 chat diagnosis

- The cancelled GTM probe reached LM Studio successfully: gateway telemetry
  recorded HTTP 200 in 13 ms and estimated 46,920 prompt tokens.
- Local LM Studio timing records for the same 10:14:54 PDT request show
  30 conversation messages and prompt processing progressing from 0% to 79.8%
  by 10:17:49. Cancellation arrived at 10:18:21 while processing was ongoing.
  This is slow prompt ingestion, not evidence of a gateway connection hang.
  The gateway token estimate is not an exact tokenizer count.
- One further synthetic no-tools request returned exactly `LOCAL_PREFILL_OK`;
  completion was observed 69 seconds after submission. This is an observation
  bound, not an exact response latency. No model, history, or provider settings
  were changed. A warm-cache effect is possible; cold latency is not cleared.
- Evidence was inspected locally from runtime timing counters and LM Studio's
  `server-logs/2026-09/2026-09-17.1.log`. No raw conversation or provider request
  payload is included in this ledger. No remote transcript export was enabled.

## Startup and health fixes

- `88c71863`: wait for authenticated gateway readiness before starting Dashboard
  config healing/agent registration. This eliminates that known startup overlap;
  the exact writer responsible for test10's earlier failure was not captured.
  Initial gateway failure now prevents a green Dashboard startup. Watchdog
  recovery failures remain retryable rather than terminating its `set -e` shell.
- `69289682`: `/api/health` checks authenticated gateway availability when
  `CLAWMAX_AUTO_START_GATEWAY=true`. Unavailable required gateways return 503,
  preserving storage counts and reporting separate gateway readiness. Standalone
  dashboards without managed autostart retain storage-only health behavior.
  Concurrent probes coalesce, cache for one second, and use a 1.5-second probe
  timeout. Public responses do not expose raw gateway error details.
- Focused evidence: entrypoint shell suite and syntax passed; server TypeScript
  passed; readiness suite reported 15 checks; gateway probe handshake and identity
  regression suites passed (four checks); changed TypeScript files passed ESLint.
- Read-only inspection of test10's current Deployment found no configured startup,
  readiness, or liveness probe. No deployment configuration was changed.

## Remaining release gates

### Build kickoff

- Full root lint/build and integration/validation/coverage gate exited 0:
  486 passed, 0 failed. Coverage: statements/lines 82.67%, branches 72.36%,
  functions 92.02%. Fresh integration chat passed and fixture cleanup completed.
- [Candidate CI](https://github.com/Maximilien-ai/clawmax/actions/runs/35261623941)
  passed at `8dcb2ee36522be34ab7ffff30837de56a2837e84`, now pinned by immutable tag
  `v2.0.0-test-rc79`.
- [Public image build](https://github.com/Maximilien-ai/clawmax/actions/runs/35268517308)
  uses `source_ref=refs/tags/v2.0.0-test-rc79` and `test_tag=rc79`.
- Ignored local `.env` now specifies `CLAWMAX_VERSION=2.0.0-test-rc79`.
  Restarted source dashboard at port 3001 reports RC79 through `/api/system`;
  its port-5174 browser UI displays the exact same version. Neither canary
  was restarted or upgraded for this local identity check.
- The coordinator `/tmp/clawmax-rc79-images.cjs` waits for the public pipeline
  to pass, checks private main remains at
  `ce2516d8a07ef96796ca138b748e6bd501672547`, then dispatches the private image
  workflow once with both `base_tag` and `image_tag` set to `2.0.0-test-rc79`.
  [Combined workflow monitor](https://github.com/Maximilien-ai/clawmax-plugins/actions/workflows/private-image.yml).
  [Combined run](https://github.com/Maximilien-ai/clawmax-plugins/actions/runs/35271827519)
  passed validation, runtime acceptance, build, and native smoke on both
  architectures. Coordinator log: `/tmp/clawmax-rc79-images.log`.
- Observed prior durations: public 32 minutes, combined 23 minutes. Coordinator
  checks halfway, expected completion plus one minute, then sparsely if overdue.
  It contains no rollout operations and stops on a failed pipeline/source mismatch.

### Recovery and canary evidence

- Both image pipelines are green, including public registry smoke and populated
  lifecycle acceptance on amd64/arm64. Combined immutable index:
  `ghcr.io/maximilien-ai/clawmax-plugins@sha256:017be1d8c648239096c2f0ae186556303688fd2b0dde76d6adfe8d474e82b82f`.
- Before upgrade, MBP14 RC78's gateway and watchdog were confirmed zombies under
  the Dashboard process and port 18789 refused connections. An explicitly approved
  restart of its Dashboard container restored authenticated connectivity without
  stopping the VM, changing its image, or losing default resource counts.
- Isolated public RC79 arm64 recovery test: image digest
  `sha256:bed1e2266021f06372d1e6b9fcf00d6ff840d5b9810b82d329ec3839a86eda88`.
  SIGTERM of its gateway caused HTTP 503 and automatic recovery to authenticated
  HTTP 200 in the observation ending 41 seconds later. A subsequent PID-verified
  SIGKILL test recovered in the observation ending 21 seconds later, replacing
  PID 541 with 823. Logs: `/tmp/clawmax-rc79-recovery.log` and
  `/tmp/clawmax-rc79-recovery-repeat-verified.log`.
- The initial second-fault harness required sampling a non-200 status and missed
  recovery faster than its ten-second sampling interval. It exited 1 despite a
  replacement PID and green health; the corrected PID-aware test above passed.
  Do not count that harness exit as a product recovery failure or silently discard it.
- Residual: the first gateway child retained one zombie entry under Dashboard.
  It did not block repeated recovery, but complete child reaping and recovery
  from loss of the watchdog itself are not established by this test. No fault
  injection was performed in either live canary. The disposable, volume-free
  `clawmax-rc79-recovery-probe` container was removed after evidence collection.
- Image pull initially failed for lack of Podman disk space (1.3 GB free on a
  40 GB filesystem). With approval, removed only unused RC76 image
  `bae84669b816d68010539b07c4cfc623fdef33393089e31f0994bc00ce541f81`;
  it is recoverable from the registry. RC77 rollback, RC78, containers, and all
  volumes were preserved. The retry passed; measured free space afterward was
  3.0 GB. Long-term disk capacity still needs attention.
- Installed worker action `mbp14-rc79-canary-20260917` completed. test10's explicit
  Kubernetes rollout completed; new pod `clawmax-dashboard-7c567fbcf8-cr6cw`
  was Running/ready with zero restarts. Both CLI status responses identify RC79.
- Both public health endpoints report `gateway.required=true`,
  `gateway.ready=true`, and HTTP 200. MBP14 default counts: 14 agents, 83 templates,
  4 groups, 5 workflows; test10 default: 13 agents, 85 templates, 11 groups,
  9 workflows. Sorted agent/workflow ID hashes match across four local and six
  cloud workspaces: `/tmp/clawmax-rc79-before.json`, `/tmp/clawmax-rc79-after.json`.
- Fresh MBP14 browser shows RC79 and an enabled conversation Download control
  once history loads. One synthetic no-tools chat returned exactly `RC79_DEMO_OK`,
  observed complete at 280 seconds. The reply persisted after closing/reopening.
  Markdown download succeeded (11,391 bytes), saved locally at
  `/tmp/clawmax-rc79-mbp14-conversation.md` with mode 0600; no transcript was
  uploaded. The response latency remains a local-model demo usability concern.
- The user completed test10's isolated-browser login, and the UI reports RC79.
  This fresh browser has no configured BYOK execution path, so Send is disabled.
  BYOK was opened for the user to configure directly; no credentials were read,
  copied from another browser, or requested in chat. Cloud fresh-chat acceptance
  remains pending, distinct from the passing gateway health and image gates.
- Post-candidate commit `c9dfd3a0` shortens the composer placeholder and exposes
  a labeled snapshot Download control, including during streaming. Export and
  composer tests, focused lint, and server TypeScript passed. Responsive visual
  acceptance remains pending. These UI changes are **not included in RC79**.

1. Local version identity passed; image validation remains separate from the
   passed local suite. Full gate log: `/tmp/clawmax-rc79-release-gate.log`.
2. Both image pipelines passed. Do not move or rebuild the immutable RC79 tag
   to include later UI changes.
3. Finish fresh chat acceptance on both installed canaries. Populated restart
   persistence passed image CI; live all-workspace identity preservation and
   gateway-aware health passed. Avoid additional pre-demo live restarts unless
   required to recover an unhealthy canary.
4. Broader test-client rollout only after canary acceptance: on-prem before cloud.
   Do not claim this change implements the missing Operations plan/apply contract.
