# Simplify + Harden + Optimize Sprint: 1.9.0

> Started: June 21, 2026
> Baseline: `v1.8.9`
> Outcome: promoted as `v1.9.0`

## Outcome Summary

`1.9.0` closed the highest-signal remaining hardening work after the `1.8.x` stabilization line:

- archived chat restore / continue
- workflow stale-notification resolution
- group chat date separators and clearer older timestamps
- workflow thread auth/runtime noise normalization
- runtime workflow channel inference for team/agent-targeted runs
- waiting-input conversation target inference
- DocHub/file-open warmup navigation fixes
- host-agent warning scoping by dashboard instance
- aligned chat/runtime diagnostic wording
- Builder AI Description fallback synthesis from user intent only

## Notes

- Validation completed on `1.9.0-test-rc5`
- Promoted after the correctly versioned `1.9.0` package/image line was rebuilt and verified
