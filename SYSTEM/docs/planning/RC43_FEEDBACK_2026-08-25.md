# RC43 Tester Feedback Tracker

> Status: active engineering tracker
> Candidate under test: `2.0.0-test-rc43`
> Updated: August 25, 2026

This tracker consolidates hands-on RC43 feedback into public-safe engineering
work. It intentionally omits raw tester prompts, customer URLs, credentials,
secret values, and private plugin implementation details. Deterministic checks
belong in engineering evidence; only human-judgment and external-environment
checks should be copied into the Release Review plugin.

## Decision

RC43 remains useful test evidence, but it is no longer promotable unchanged.
The LM Studio and QBO fixes modify runtime source and packaging, so the next
candidate must be RC44 after the focused fixes below are complete and the full
public/private image gate passes.

## Complete Feedback Register

| ID | Finding | Disposition | Priority | Status | Acceptance evidence |
|---|---|---|---|---|---|
| F01 | An LM Studio Gemma QAT model was selected in ClawMax but rejected by OpenClaw as not allowed for the agent. | Authorize the exact `lmstudio/...` execution reference while preserving the saved `openai-compatible/...` workspace selection. | P0 | Source fixed in `fe1a2680`; hosted CI and RC44 image pending | TypeScript passes; agent-execution suite passes 48/48 with the reported Gemma model shape; live LM Studio chat must succeed in RC44. |
| F02 | The QBO skill displayed a Go installer that the Linux image could not execute. | Package pinned QBO v0.6.1 binaries for Linux amd64/arm64 with hardcoded upstream checksums and retained license. | P0 | Source fixed in `fcb15ae8`; multi-architecture RC44 image pending | Dockerfile contract passes; both image architectures must report `qbo --version`; QBO skill must not request a missing compiler after restart. |
| F03 | Builder’s introductory instruction is styled like an input, so users try to type into it. | Restyle it as a compact onboarding callout with an explicit “How Builder works” label. | P1 | Planned today | Desktop/mobile visual test distinguishes instruction from the editable prompt. |
| F04 | The actual Builder prompt can start below the fold. | Put the editable prompt before transcript/recommendation history on first use and preserve responsive scrolling after conversation begins. | P1 | Planned today | At supported desktop and mobile heights, a new user can see the prompt and primary action without scrolling. |
| F05 | New users cannot tell whether to describe an outcome, command a main agent, or create a sub-agent. | Explain that Builder routes an outcome into an existing asset or an explicit creation flow; it does not command a hidden main agent or silently create sub-agents. | P1 | Planned today | First-use copy states what to enter, what Builder changes, and what requires confirmation. |
| F06 | Builder extracted “it to” from ordinary prose as the requested agent name instead of honoring the explicit name. | Harden explicit-name parsing and exclude pronoun/action fragments from agent-name matches. | P0 | Planned today | Regression prompt resolves the requested name and never produces “it to.” |
| F07 | Builder offered “Chat with” a nonexistent agent and then opened an empty Agents page. | Never emit chat/reuse actions unless the matched workspace agent has a real ID; route create intent to an explicit AI Generate Agent action. | P0 | Planned today | A zero-agent workspace receives a create action; clicking it opens a prefilled creation flow, not an empty list. |
| F08 | “Improve with AI” reduced the local prompt-readiness score without explaining why. | Keep readiness advisory and local; show before/after score and rubric changes before replacement, and provide restore/undo. Do not promise monotonic improvement. | P1 | Investigate today; interaction may follow RC44 if larger than a focused fix | Test covers score decrease, explicit user acceptance, and restoration without persisting raw prompt feedback. |
| F09 | “Design This” recommended an existing workspace agent even when the workspace had zero agents. | Make inventory a hard routing constraint rather than a confidence hint. | P0 | Planned today with F07 | Zero-agent unit/browser regression cannot return `existing_agent` or `reuse_existing`. |
| F10 | “Review agent skills” opened an apparently unrelated Luma skill with no explanation and looked like a silent prerequisite failure. | Label skill matches as optional capability suggestions, show the match rationale, and never imply that opening a suggestion created or failed an agent. | P1 | Planned today | Recommendation names why the skill matched and clearly separates “optional setup” from agent creation state. |
| F11 | Luma’s key/setup banner made it appear impossible to create the agent without every listed value. | State that creation is allowed; only that optional skill capability remains unavailable until one supported input path is configured. | P1 | Planned today | Skill dialog and Builder handoff distinguish create-now, configure-later, and required-at-run behavior. |
| F12 | Skills, OpenClaw plugins, ClawMax plugins, and partner integrations appear interchangeable. | Add concise product-language definitions at the Skills partner installer and Partners configuration surfaces. | P1 | Planned today | Copy explains: skills are agent instructions/tools; OpenClaw plugins extend the runtime; ClawMax plugins extend the dashboard; partner integrations configure external services. |
| F13 | Partner plugin installation says to restart if needed but provides no location or action. | Reinspect plugin status automatically after installation; when restart is truly required, provide environment-specific instructions or an operator action instead of a vague conditional. | P1 | Planned today | Successful install reports discovered/enabled status; unresolved refresh state has an actionable next step. |
| F14 | The top-level Partners button opens configuration while Cognee installation is under Skills, with no cross-link or distinction. | Rename/describe the top action as partner configuration and add reciprocal navigation between partner configuration and partner plugin installation. | P1 | Planned today | A user can locate both configuration and installation and understands why they are separate. |
| F15 | The Partners stepper makes Opik and Resend look required before Cognee. | Replace prerequisite-like sequencing with independent optional sections/tabs and explicitly state that Cognee does not require Opik or Resend. | P1 | Planned today | Cognee can be selected/configured directly; copy and navigation do not imply ordered prerequisites. |
| F16 | RC43 has no packaged current checklist in Review; only the RC39 suggested checklist is present. | Add an RC44 checklist containing only human-judgment and external-environment checks, including these repaired journeys. | P0 for RC44 | Pending after fixes stabilize | Review initializes one focused RC44 set, preserves earlier evidence in Archived, and exports the completed review. |
| F17 | The long Podman soak retains process/resource state across time. | Continue issue [#187](https://github.com/Maximilien-ai/clawmax/issues/187); do not conflate it with the UI fixes above. | P0 launch gate | Open | Repeat the accepted-duration soak and record memory/PID baseline, peak, end state, restart recovery, owner, and disposition. |
| F18 | Alternate Claude Code/Droid runtime work remains a large conflicting PR. | Keep PR [#170](https://github.com/Maximilien-ai/clawmax/pull/170) post-2.0 and split it into independently reviewable concerns. | Post-2.0 | Disposition recorded | No RC44 dependency; focused follow-up PRs rebase on stable 2.0 and restore hosted checks. |

## Today’s Execution Order

1. Land F01 and F02 as independent tested commits.
2. Repair the zero-agent Builder path as one behavior concern: F06, F07, and F09.
3. Repair first-use Builder presentation and guidance: F03, F04, and F05.
4. Clarify optional skill recommendations and setup: F10 and F11.
5. Clarify and connect partner installation/configuration: F12 through F15.
6. Investigate F08 without weakening the local advisory scoring boundary.
7. Create the focused RC44 Review checklist only after the repaired journeys are stable.
8. Run the complete integration, validation, coverage, visual, public/private boundary, and multi-architecture image gates before offering RC44.

## Release Boundary

- F01 through F17 are evaluated for RC44 or explicit launch deferral.
- F18 remains post-2.0.
- Documentation-only GitHub issues remain deferred until the 2.0 release is live.
- No item is considered complete merely because copy changed: every user-visible
  operation must retain status, history, or evidence showing what happened.
