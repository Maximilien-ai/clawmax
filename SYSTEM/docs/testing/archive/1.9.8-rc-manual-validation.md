# 1.9.8 RC Manual Validation

> Archived outcome: `1.9.8-test-rc5` was promoted as `v1.9.8` on July 18, 2026. Remaining unchecked items are non-blocking follow-up coverage, not active release gates.

Living checklist for local and containerized cloud/on-prem validation. Remove confirmed sections as the release candidate is exercised; retain failures and untested cases until resolved.

Status key: `[x]` confirmed, `[ ]` pending, `[!]` retest after a fix.

## Release identity

- [x] Dashboard and `/api/system` report `1.9.8-test-rc2`.
- [x] OpenClaw reports the expected `v2026.6.11` baseline.
- [x] Restarting the container preserves workspace and OpenClaw state.
- [ ] Repeat release identity checks with `1.9.8-test-rc5`.

## Federated skill search

- [x] Broad searches such as `github`, `productivity`, and `api` return results.
- [x] Results appear from ClawHub, Shipables, and Tessl when available.
- [x] Each result identifies its registry.
- [x] Registry filters narrow the results.
- [x] Installation uses the result's original registry.
- [x] An imported skill appears in the workspace and can be assigned to an agent.
- [ ] Force one registry to fail and confirm results from the other registries still load. RC2 did not exercise a forced failure.
- [ ] Find duplicate cross-registry results and confirm the choices are understandable and install the intended package. RC2 did not expose duplicates.
- [!] Confirm long dynamic result names and descriptions wrap without colliding with the Install button in RC5. RC2 reproduced the formatting problem in `Screenshot 2026-07-16 at 9.54.12 AM.png`.
- [ ] Investigate whether Shipables is disproportionately represented or whether the tested terms naturally favor its catalog.

## DocHub bulk file management

- [x] Upload multiple files into an agent directory.
- [x] Multi-select uploaded files.
- [x] Move selected files into a subdirectory of that agent.
- [x] Files cannot move outside their original agent boundary.
- [x] Bulk deletion removes selected uploaded files after confirmation.
- [x] Agent-created and pre-existing files cannot be bulk deleted.
- [ ] Repeat upload, move, and boundary protection inside `ORG` or `SYSTEM`.
- [!] Move uploaded images and confirm they remain labeled `asset` in RC5. RC2 displayed them as green `memory` entries after moving. RC3 crashed DocHub when rendering a synthetic directory entry without a path (`Cannot read properties of undefined (reading 'toLowerCase')`); see `Screenshot 2026-07-16 at 12.27.22 PM.png`.
- [x] RC5 keeps the selected-file count plus `Move selected`, `Delete selected`, and clear actions visible while scrolling the document tree. RC4 allowed the action island to scroll out of view.
- [ ] Confirm moved uploaded files remain selectable, movable, and deletable after refreshing the page and restarting the container.

## Named exports

- [ ] Export an organization template and confirm the archive filename uses the template name.
- [ ] Export an agent and confirm the archive filename uses the agent name.
- [ ] Export a skill and confirm the archive filename uses the skill name.
- [ ] Export a workflow and confirm the archive filename uses the workflow name.
- [ ] Confirm exported filenames are safe for spaces, punctuation, and duplicate downloads.

## Dashboard navigation memory

- [ ] Navigate away from Builder, refresh, and confirm the same page is restored.
- [ ] Restart the browser and confirm the last page is restored when still valid.
- [ ] Confirm a removed or invalid saved page falls back to Builder.
- [ ] Change System section expansion state, refresh, and confirm it is restored.
- [ ] Verify navigation and section state independently for two browser profiles/users.

## Runtime regressions

- [ ] Chat with a supported agent locally and in a container.
- [ ] Chat with an unsupported-model agent and confirm the message names the model, explains the problem, and links to agent editing.
- [ ] Run group chat with supported agents.
- [ ] Run a sequential workflow and verify every step completes.
- [ ] Run a DAG/parallel workflow and verify progress, dependencies, and outputs.
- [ ] Restart during or after workflow execution and verify execution history remains coherent.
- [ ] Verify plugin environment/configuration survives container startup without exposing private plugin bundles.
- [ ] Review container logs for gateway restarts, session conflicts, unsupported models, and unhandled errors.
