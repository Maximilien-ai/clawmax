{
  "apiVersion": "clawmax.ai/v2",
  "id": "plugin-lab-review-notes",
  "slug": "plugin-lab-review-notes",
  "name": "Release Review",
  "description": "Run acceptance checklists independently for each release, capture reviewer evidence and notes, and export a complete report for follow-up.",
  "version": "0.3.0-test",
  "icon": "checklist",
  "objectKind": "review-note",
  "visibility": "private",
  "enabledByDefault": false,
  "source": {
    "type": "github",
    "owner": "Maximilien-ai",
    "repo": "clawmax",
    "url": "https://github.com/Maximilien-ai/clawmax/tree/main/PLUGINS/test/plugin-lab-review-notes",
    "branch": "main"
  },
  "nav": {
    "section": "plugins",
    "order": 1000,
    "label": "Review"
  },
  "capabilities": {
    "notifications": true,
    "docs": true
  },
  "labels": {
    "singular": "Check",
    "plural": "Checks"
  },
  "recordSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["release", "area", "outcome"],
    "properties": {
      "release": {
        "type": "string",
        "title": "Release"
      },
      "area": {
        "type": "string",
        "title": "Area",
        "enum": ["identity", "navigation", "plugins", "guardrails", "evals", "persistence", "responsive", "regression"],
        "default": "regression"
      },
      "completed": {
        "type": "boolean",
        "title": "Completed",
        "default": false
      },
      "outcome": {
        "type": "string",
        "title": "Outcome",
        "enum": ["pending", "passed", "failed", "blocked"],
        "default": "pending"
      },
      "notes": {
        "type": "string",
        "title": "Notes",
        "format": "textarea"
      },
      "owner": {
        "type": "string",
        "title": "Owner"
      },
      "evidence": {
        "type": "array",
        "title": "Evidence",
        "items": { "type": "string" },
        "default": []
      },
      "verifiedBy": {
        "type": "array",
        "title": "Previously verified by",
        "description": "Tester, environment, and date from an earlier exported review.",
        "items": { "type": "string" },
        "default": []
      }
    }
  },
  "ui": {
    "form": {
      "order": ["release", "area", "completed", "outcome", "notes", "owner", "evidence", "verifiedBy"]
    },
    "list": {
      "fields": ["area", "outcome", "completed", "owner", "evidence", "verifiedBy"],
      "groupBy": "release",
      "checkField": "completed"
    }
  }
}
